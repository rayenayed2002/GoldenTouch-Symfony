<form method="post" action="{{ path('app_perso_delete', {'idP': personnel.idP}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personnel.idP) }}">
    <button class="btn">Delete</button>
</form>
