<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>All Reservations</title>
    <style>
        table { border-collapse: collapse; width: 100%; }
        th, td { border: 1px solid #ddd; padding: 8px; }
        th { background-color: #f2f2f2; }
        tr:nth-child(even) { background-color: #f9f9f9; }
    </style>
</head>
<body>
    <h1>All Reservations</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>User ID</th>
                <th>Lieu</th>
                <th>Date</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
        {% for reservation in reservations %}
            <tr>
                <td>{{ reservation.id }}</td>
                <td>{{ reservation.userId }}</td>
                <td>{{ reservation.lieuId ? reservation.lieuId.name : 'N/A' }}</td>
                <td>{{ reservation.dateReservation ? reservation.dateReservation|date('d/m/Y H:i') : 'N/A' }}</td>
                <td>{{ reservation.status|default('pending') }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5" style="text-align:center;">No reservations found.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</body>
</html>